services:
  - type: web
    name: uplab-serveur
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn serveurC:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
